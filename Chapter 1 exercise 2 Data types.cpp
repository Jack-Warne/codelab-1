#include <iostream>
#include <string>
using namespace std;

int main() {
    int firstNumber = 1; //numerical value so must be integer (int)
    bool iCanCode = true; // boolean variable so assigned to bool
    char hopefulGrade = 'a'; // character so assigned char
    double myDecimal = 1.0; // as it is a rational set as a double 
    char const* minimalSentance = "y"; // constant character can't change so char const*
    int keyMash = 13213123; // a large numerical value so assigned int
    float mysteryDataType = 5.6f; //Use Google to work this out

    return 0;
}
